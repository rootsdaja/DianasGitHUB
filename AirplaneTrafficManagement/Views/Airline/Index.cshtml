@model IEnumerable<AirplaneTrafficManagement.Database.Airline>
@{
    ViewBag.Title = "Airline";
}

<hgroup class="title">
    <h1>@ViewBag.Title.</h1>
    <h2>Display all the Airlines</h2>
</hgroup>


<table class="table table-bordered table-hover">
    <tr>
        <th><b>Id Airline</b></th>
        <th><b>Company Name</b></th>
        <th><b>Company Logo</b></th>
        <th><b>Options</b></th>
    </tr>

    @foreach (AirplaneTrafficManagement.Database.Airline item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.idAirline)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.companyName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.logo)
           </td>
            <td>
                @*@{var session = new Common.WebSessionContext();
                    }

                    @if (session.Admin == true)
                    {*@

                @Html.ActionLink("Edit", "EditAirline", "Airline", new { id = item.idAirline }, null) |
                @Html.ActionLink("Delete", "DeleteAirline", "Airline", new { id = item.idAirline }, null) |
                @Html.ActionLink("Add", "CreateAirline", "Airline", null) 

                @*}

                    else
                    {*@

                @*@Html.ActionLink("Flight Details", "movieDetail", "Flight", new { id_detail = item.idFlight }, null)
                    <p>@Html.ActionLink("Reserve Ticket", "CreateTicket", "Flight", new { id = item.idFlight }, null)  </p>*@

                @*}*@

            </td>
        </tr>
    }

</table>


